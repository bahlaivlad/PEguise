.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/
# IDE state (workspace.xml carries absolute local paths).
.idea/
# Generated by tests/make_fixtures.py -- rebuilt on every test run.
tests/fixtures/
# Sample archives (VirusTotal batches and similar) must never be committed.
# Keep live samples outside the tree, or in an encrypted container.
*.zip
*.7z
*.rar
# Personal Claude Code permissions; not shared.
.claude/settings.local.json
# Draft external reports with real certificate identifiers; kept local by decision.
scratch_reports/
